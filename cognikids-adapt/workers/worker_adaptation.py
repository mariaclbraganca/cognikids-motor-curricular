# TODO: consome a fila 'adaptation' (mensagem já chega com job + analysis +
# profile_tokens, publicada por worker_profile.py), chama o LLM (Gemma 4 12B
# local via Ollama, fallback Gemini API — ADR-002 já decidida) e grava as
# versões adaptadas em curriculum_jobs — ver CLAUDE.md seção 8 (contrato
# /v1/curriculum/adapt). Fica para quando entrarmos na parte do LLM.
