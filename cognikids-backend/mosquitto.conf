# Configuração do Mosquitto MQTT Broker

# Permite conexões sem autenticação (para desenvolvimento)
allow_anonymous true

# Listener na porta padrão MQTT
listener 1883

# Listener WebSocket (opcional)
listener 9001
protocol websockets

# Log de depuração
log_dest stdout
log_type all
